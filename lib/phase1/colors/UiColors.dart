import 'package:flutter/material.dart';

class Uicolor {
  static Color appBar = Colors.teal;  // Example purple color
  static Color body = Colors.teal.shade400;  // Example teal color
  static const Color background = Color(0xFFF2F2F2);  // Light background
  static const Color text = Color(0xFF000000);  // Black text color
  static const Color error = Color(0xFFB00020);  // Error color (red)
  static const Color success = Color(0xFF4CAF50);  // Success color (green)
  static const Color warning = Color(0xFFFFC107);  // Warning color (amber)
  static const Color splash_logo_color = Color(0xFF008080);
}
